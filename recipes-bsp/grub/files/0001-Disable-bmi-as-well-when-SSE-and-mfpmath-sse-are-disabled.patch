--- a/configure.ac.orig	2022-07-26 14:16:02.018695871 +0200
+++ b/configure.ac	2022-07-26 14:16:49.374342972 +0200
@@ -824,7 +824,7 @@ fi
 if ( test "x$target_cpu" = xi386 || test "x$target_cpu" = xx86_64 ) && test "x$platform" != xemu; then
   # Some toolchains enable these features by default, but they need
   # registers that aren't set up properly in GRUB.
-  TARGET_CFLAGS="$TARGET_CFLAGS -mno-mmx -mno-sse -mno-sse2 -mno-sse3 -mno-3dnow -mfpmath=387"
+  TARGET_CFLAGS="$TARGET_CFLAGS -mno-mmx -mno-sse -mno-sse2 -mno-sse3 -mno-bmi -mno-3dnow -mfpmath=387"
 fi
 
 # GRUB doesn't use float or doubles at all. Yet some toolchains may decide
